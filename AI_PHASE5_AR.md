# AI Phase 5 — Diana

تم تحسين مسار الرسائل بحيث يظل المستخدم قادرًا على إلغاء الرد من خلال التدفق الحالي، مع إضافة مهلة 195 ثانية تمنع بقاء الواجهة في حالة THINKING إلى أجل غير محدد. عند انتهاء المهلة تظهر حالة `TIMEOUT // TRY AGAIN` ولا يتم تسجيل رسالة خطأ غامضة باسم OFFLINE.

أضيف أيضًا fallback محلي لكل شخصية. Texty يبدأ بـ `qwen3:8b` ثم يحاول `diana-texty:latest` و`qwen2.5:7b` إذا كان النموذج الأساسي غير موجود. Coding يبدأ بـ `qwen2.5-coder:7b` ثم يحاول `diana-coding:latest` و`qwen2.5:7b`. يتم فحص `/api/tags` مع cache قصير، ولا يتم تشغيل Ollama أو تعطيل Diana إذا كانت الخدمة غير متاحة.

يرجع backend اسم النموذج المستخدم في استجابة chat حتى يمكن عرض التشخيص لاحقًا. السجل العام والخاص يظل منفصلًا، ولا يتم تغيير اختيار الشخصية أو تجربة الواجهة.

حدود المرحلة: الـ API الحالي ما زال يرسل الرد كاملًا بدل streaming token-by-token. هذا قرار محافظ لتجنب كسر history وTTS؛ يمكن تنفيذ streaming كمرحلة مستقلة بعد تثبيت Installer على Windows.

تم اجتياز Python syntax وrenderer JavaScript syntax بعد هذه التغييرات.
