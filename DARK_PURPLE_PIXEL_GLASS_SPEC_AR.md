# Diana — Dark Purple Pixel Glass Design Specification

## الرؤية

الهوية الجديدة هي مزيج بين **Glassmorphism هادئ** و**Pixel Art واضح**. الزجاج يظهر في الحاويات واللوحات والخلفيات، بينما النصوص والكود والأيقونات تبقى حادة ومقروءة. لا يُستخدم `blur` على النص أو الكود، ولا تُستخدم scanlines كثيفة تجعل الشاشة تبدو تالفة.

## لوحة الألوان

| الدور | القيمة المقترحة | الاستخدام |
|---|---|---|
| Deep Space | `#090612` | خلفية التطبيق الأساسية |
| Violet Void | `#120B24` | طبقات الخلفية والزجاج الداكن |
| Glass Violet | `rgba(45, 24, 83, .42)` | البطاقات واللوحات |
| Electric Violet | `#9B7BFF` | الحالة النشطة والحواف |
| Orchid | `#D7B8FF` | العناوين والنصوص المهمة |
| Pixel Mint | `#A6FFE0` | نجاح/جاهزية/روابط محلية |
| Warm Amber | `#FFD18A` | تحذير وloading |
| Soft Red | `#FF8FAE` | الأخطاء والحذف |
| Muted Text | `rgba(232, 220, 255, .68)` | النص الثانوي |

## Typography

النصوص الأساسية تستخدم Pixel font واضحة ذات وزن متوسط ومسافات مريحة. العناوين والlabels يمكن أن تكون Pixel بالكامل. النصوص الطويلة في الشات تستخدم Pixel-inspired fallback أكثر راحة عند الأحجام الصغيرة، مع `font-smoothing` وline-height مناسبين. الكود لا يتأثر بـ pixelation أو blur.

## الزجاج

الحاويات تستخدم خلفية شفافة، حدًا بنفسجيًا رفيعًا، و`backdrop-filter: blur(18px) saturate(125%)` عند توفره. عند عدم توفر blur يوجد fallback بلون ثابت قريب من `#160D2B`. الزجاج لا يستخدم opacity منخفضة جدًا؛ يجب أن يظل النص واضحًا فوقه. الحواف مستديرة باعتدال، وليست كل العناصر pills.

## شعار D-Core Orbital Glass

الشعار الجديد يتكون من silhouette حرف D بكسلي نظيف، وفي داخله orb بنفسجي شفاف، نقطة مركزية، وحلقة Saturn واحدة دقيقة تدور حول النقطة. الحلقة هي العنصر المتحرك الأساسي؛ الـ orb يتنفس بإضاءة خفيفة فقط. يمكن وضع ثلاث عقد Pixel صغيرة داخل orb كإشارة إلى Texty وCoding وAgent، بدون إضافة نص داخل الشعار.

## الحركة

الحركة تشبه iPhone في النعومة: easing من نوع cubic-bezier، مدة قصيرة للحالات اليومية، وspring-like reveal للبطاقات. لا توجد اهتزازات أو flicker مستمر. الـ sidebar يفتح بانزلاق خفيف مع opacity، الـ panels تدخل من الأسفل 8–12px، والـ Saturn يدور بسرعة بطيئة ثابتة. `prefers-reduced-motion` يلغي الدوران والانتقالات الثقيلة.

## Main App

الواجهة الرئيسية تستخدم شات مركزيًا عريضًا، sidebar زجاجيًا، وcontrol dock جانبيًا للـ levers. الحالة المحلية تظهر في status chip صغير: `LOCAL // OLLAMA // READY`. لا تظهر readouts عشوائية إلا كعنصر زخرفي محدود في Home، وليس خلف النصوص.

## IDE

يحافظ IDE على ثلاثة أعمدة: Explorer، Monaco، وDiana Chat. Explorer وChat زجاجيان، Monaco على سطح بنفسجي أكثر عتامة ووضوحًا، وterminal الداخلي bottom drawer بنفس الثيم. شريط الأدوات العلوي موحد مع main app. ألوان syntax لا تتعارض مع الخلفية البنفسجية، ورسائل الخطأ تظهر في بطاقة واضحة ذات حد Soft Red.

## قواعد عدم التراجع

لا يتم تغيير IPC أو منطق المحادثات أو التخزين أثناء تعديل الثيم إلا عند الضرورة البصرية. كل تعديل بصري يضاف في طبقة CSS واضحة، مع backup قبل البدء. يجب اختبار الأزرار والـ overlays بعد كل مرحلة، لأن ظهور العنصر لا يكفي لإثبات قابلية النقر.
