# Diana — المرحلة الأولى: أمان IPC ومسارات الملفات

تم تأمين عمليات الملفات في Electron main process مع الحفاظ على الاستخدام الطبيعي داخل IDE.

## السياسة الجديدة

عند اختيار مجلد من `OPEN FOLDER`، يتم تسجيله كجذر مصرح به للجلسة. عمليات `readTree` و`statPath` و`readFile` و`writeFile` تعمل داخل هذا الجذر فقط.

عند سحب ملف أو مجلد إلى IDE، يظهر طلب إذن من Diana قبل الوصول إليه. بعد الموافقة يتم السماح بالعنصر ومحتوياته خلال الجلسة.

إنشاء ملف جديد أو مجلد جديد يحتاج إلى موافقة منفصلة من نافذة النظام. تعديل ملف موجود داخل مجلد مصرح به لا يعرض تأكيدًا متكررًا في كل عملية حفظ.

## الحماية المضافة

- رفض المسارات خارج المجلدات التي وافق عليها المستخدم.
- منع تجاوز المسار باستخدام `..`.
- فحص المسار الحقيقي لمنع استخدام symlink للوصول إلى مكان خارج الجذر المصرح.
- حد أقصى لحجم نص الملف أو مدخلات الطرفية في طلب IPC واحد.
- ربط جلسة الطرفية بنافذة Electron التي أنشأتها، لمنع نافذة أخرى من التحكم فيها.
- رسائل `ACCESS BLOCKED` ورفض واضحة داخل IDE.
- إضافة `createDirectory` إلى preload للاستخدام الآمن مستقبلًا.

## الاختبار

تم اجتياز فحص syntax لملفات `main.cjs` و`preload.cjs` و`ide-renderer.js` وJavaScript المضمن في `index.html`، بالإضافة إلى فحص Python ووجود علامات الحماية المطلوبة.

لم يتم إجراء اختبار Electron/Windows فعلي داخل بيئة Linux الحالية؛ لذلك يجب اختبار اختيار مجلد، سحب ملف، الحفظ، رفض الإنشاء، وتشغيل الطرفية على Windows بعد استبدال النسخة.

> الإذن مؤقت خلال جلسة التطبيق ولا يعني أن Diana تستطيع الوصول إلى كل ملفات الجهاز.
