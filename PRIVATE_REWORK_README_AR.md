# Diana // Private Chat Rework

هذه النسخة مبنية على آخر نسخة سليمة مرفقة، وتم الحفاظ على شكلها الأصلي. التعديل يخص Pass Key وPrivate Chat فقط.

## فتح المحادثة الخاصة

1. افتح الشريط الجانبي.
2. اضغط على خيار `Pass Key` مع الاستمرار على `Shift`.
3. سيظهر المنشن داخل الشات باسم `PASS KEY`.
4. اكتب الكود الخاص بك واضغط Enter.
5. سيبدأ `PRIVATE SESSION` منفصلًا بصريًا عن الجلسة العامة، ولن يظهر شريط الخاص قبل التحقق.

يمكن أيضًا فتح لوحة Pass Key نفسها من الخيار بدون Shift، ثم إدخال الكود لعرض Private History.

## القفل والمسح

أثناء private session يظهر شريط `PRIVATE LINK // PASS VERIFIED // LOCAL ONLY` وبداخله `LOCK` و`ZAP`. زر `LOCK` يخفي الجلسة ويرجع لشات عام فارغ. زر `ZAP` يطلب تأكيدًا ثم يمسح private chat/code/file history فقط ولا يمسح الذاكرة العامة.

اختصار ZAP التاريخي `Z + ESC` ما زال موجودًا. إذا كان private session فعالًا، يمسح الخاص فقط؛ خارج الخاص يعمل على الذاكرة العامة كما كان.

## الخصوصية

المحادثة الخاصة معزولة عن العرض العام ولا يتم إظهار private history إلا بعد Pass Key داخل هذا التشغيل. التخزين الحالي في `localStorage` محلي وغير مشفر؛ لذلك هذه حماية واجهة/جلسة وليست خزنة تشفير كاملة. لا تُعامل النسخة كحل أمني للمعلومات شديدة الحساسية قبل إضافة Windows DPAPI أو Credential Manager.
